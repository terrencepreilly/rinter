/* A good function has a comment to describe its function.  If the comment
   is too long, it is broken manually. */
int agoodfunction(int a)
{
   long int random(void);
   int i;
   int z = 0;

   while (i < a)
      i = (int) random();

   return i;
}
